{
    "scripts": {
        "start": "npx @11ty/eleventy --serve & npx tailwindcss -i styles/tailwind.css -c styles/tailwind.config.js -o _site/style.css --watch",
        "build": "ELEVENTY_PRODUCTION=true npx @11ty/eleventy & NODE_ENV=production npx tailwindcss -i styles/tailwind.css -c styles/tailwind.config.js -o _site/style.css --minify"
    },
    "devDependencies": {
        "@11ty/eleventy": "^0.12.1",
        "alpinejs": "^3.3.3",
        "autoprefixer": "^10.3.6",
        "html-minifier": "^4.0.0",
        "postcss": "^8.3.8",
        "tailwindcss": "^2.2.16"
    }
}